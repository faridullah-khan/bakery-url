{
  "name": "bakery-pos-functions",
  "engines": {
    "node": "18"
  },
  "scripts": {
    "lint": "eslint . || true",
    "serve": "firebase emulators:start --only functions,hosting",
    "deploy": "firebase deploy --only functions",
    "build": "echo 'Functions are deployed directly; no build step'"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^4.4.1"
  }
}

